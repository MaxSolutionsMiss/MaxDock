MaxDock DB73 — Shared Layout and Screenshot Repair

Rollback references
- DB72 source: 1afde80a048668b60a6ba5f45de1b6290808c354
- DB72 GitHub Pages deployment: a48fc63bfee8890449884eaf1def8bbb620bb26f

Scope
- Keep My Appointments as the reference for page-title Export/Print placement.
- Keep page-specific settings gears at the far-right end of their control row.
- Restore one 78 px horizontal KPI-card pattern across operational metrics and User Management.
- Separate the Operations Queue controls from the page-title row.
- Remove the obsolete Dashboard overview wrapper that caused controls and metrics to collide.
- Render solid gear icons correctly.
- Keep Settings tabs functional with Save Settings and Reset Defaults in every panel.
- Make Data Integration fields conditional on Daily CSV versus secure bridge selection.
- Use the available Data Integration workspace width at desktop sizes.
- Start the shared layout at DOMContentLoaded to reduce shell/layout delay.

Runtime safeguards
- One DB73 controller is the final DOM authority.
- Legacy shared MutationObservers are disconnected before DB73 takes ownership.
- One scoped native observer reacts only to child-list changes.
- No new numbered CSS or JavaScript patch asset is introduced.
- Root and db04 delivery files remain byte-identical.

Database boundary
- No Supabase migration.
- No Auth configuration change.
- No Edge Function deployment.
- No production deployment in the review pull request.

Validation
- JavaScript syntax checks.
- CSS parser validation.
- Seven-page JSDOM runtime contract.
- Settings tab and per-panel action checks.
- HTML duplicate-ID, label-target, and asset-reference checks.
- Root/db04 byte parity.
- DB73 cache-marker and no-numbered-patch checks.
